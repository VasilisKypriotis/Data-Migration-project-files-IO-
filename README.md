The program in the begging accepts a csv file ( with the command sys ), this file contains some users that tried to send their data.
The code must be able to write again the names of the users properly ( only the first letter must be capital ) and rewrite the phone
numbers without spaces or dashes ("-"). The most important factor is the email, if the given email from the user doesn't contain the 
"@", the whole login info must be saved automatically in a new csv file "No_emails_users", else the login info must be saved 
automatically in another csv file "New_users".
